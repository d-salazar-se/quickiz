package grupo.uno.quickiz.designPatterns;

public class ModelFactory {
	
	/*use getShape method to get object of type shape 
   public Model getModel(String modelName){
      if(modelName == null){
         return null;
      }		
      if(modelName.equalsIgnoreCase("Question")){
         return new Question();
         
      } else if(modelName.equalsIgnoreCase("Pool")){
    	  Pool pool = new Pool();
         return pool;
         
      } else if(modelName.equalsIgnoreCase("Quiz")){
         return new Quiz();
         
      } else if(modelName.equalsIgnoreCase("QuizStructure")){
          return new QuizStructure();
      }
      
      return null;
   }*/

}
